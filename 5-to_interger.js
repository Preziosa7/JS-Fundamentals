#!/usr/bin/node

// Define an array with the required strings
const languages = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

// Use a loop to iterate and print each element with a single console.log
for (const lang of languages) console.log(lang);
